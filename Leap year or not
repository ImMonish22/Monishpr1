#include <stdio.h>

int main()
{
    int n;
    printf("Enter the year:");
    scanf("%d",&n);
    if(n%4==0 && n%400==0||n%100!=0)
    {
        printf("\n%d Is a leap year",n);
    }
    else 
    {
        printf("\n%d It is not a leap year",n);
    }
    return 0;
}
